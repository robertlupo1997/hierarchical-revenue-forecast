apiVersion: 1

providers:
  - name: 'MLRF Dashboards'
    orgId: 1
    folder: 'MLRF'
    folderUid: 'mlrf'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
